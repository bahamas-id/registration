name: 'Delete old artifacts'

on:
  schedule:
    - cron: '0 * * * *' # every hour

jobs:
  delete-artifacts:
    permissions:
      actions: write
    uses: mosip/kattu/.github/workflows/clear-artifacts.yml@master
